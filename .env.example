# ── Database ──
DB_PASSWORD=your_secure_password_here

# ── Backend ──
# Comma-separated origins, or leave empty to allow all (nginx handles security)
CORS_ORIGIN=

# ── Frontend ──
# Set to your domain; leave as /api for Docker nginx setup
VITE_API_URL=/api
